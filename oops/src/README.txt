final variable- once declared a final variable, we cannot change its value in runtime
super - super always points to parent most class variable
this -  points to this class variable
super and this cannot be used in static things(because we call static by just class reference)
we create reference of webdriver by invoking constructor of its child class(chromedriver)
findelement returns webelement
findelements returns list of webelements